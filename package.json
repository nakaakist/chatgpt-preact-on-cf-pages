{
  "private": true,
  "scripts": {
    "start": "wrangler pages dev -- wmr",
    "build": "wmr build --prerender",
    "serve": "wmr serve",
    "format": "prettier --write ."
  },
  "eslintConfig": {
    "extends": "preact"
  },
  "dependencies": {
    "preact": "^10.13.0",
    "preact-iso": "^2.3.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20230228.0",
    "prettier": "^2.8.4",
    "typescript": "^4.9.5",
    "wmr": "^3.8.0",
    "wrangler": "^2.12.0"
  }
}
